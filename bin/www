#!/usr/bin/env node

var app = require('./../app');
app.set('port', (process.env.PORT || 3000))
app.listen(app.get('port'), function(){
    console.log('BuildingBlocks app is running at http://localhost:' + app.get('port'));
});